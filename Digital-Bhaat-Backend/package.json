{
  "name": "serverless-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --ext ts --exec 'npx serverless offline start --stage development' --signal SIGTERM",
    "start":"nodemon --ext ts --exec 'npx serverless offline start --stage production' --signal SIGTERM",
    "deploy":"npx serverless deploy --stage production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.0",
    "nodemon": "^3.1.10",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.15.1",
    "serverless": "^4.17.0",
    "serverless-http": "^3.2.0",
    "serverless-offline": "^14.4.0"
  }
}
